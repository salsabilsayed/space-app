import React from 'react'

const Title = props => {
    return (
        <div className="text-white font-bellefair uppercase">
            {props.children}
        </div>
    )
}

export default Title;
